import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Vendors: React.FC = () => {
  return (
    <div>
      <Title level={1}>廠商管理</Title>
      <Card>
        <p>廠商管理功能開發中...</p>
      </Card>
    </div>
  );
};

export default Vendors;