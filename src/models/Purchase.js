const { DataTypes } = require('sequelize'); module.exports = (sequelize) => sequelize.define('Purchase', { id: { type: DataTypes.INTEGER, primaryKey: true, autoIncrement: true } }, { tableName: 'purchases', timestamps: true });
